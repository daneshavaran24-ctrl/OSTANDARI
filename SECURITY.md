# امنیت

## گزارش آسیب‌پذیری

اگر آسیب‌پذیری‌ای پیدا کردید، **آن را به‌صورت issue عمومی ثبت نکنید.** مستقیم به
نگهدارنده‌ی مخزن پیام بدهید و فرصت رفع بدهید.

در گزارش این‌ها را بنویسید: مسیر رسیدن به مشکل، اثر عملی‌اش، و اگر می‌شود یک
نمونه‌ی بازتولید.

---

## مدل تهدید

این سامانه سه مرز اعتماد دارد:

| مرز | چه چیزی غیرقابل‌اعتماد است |
|---|---|
| مرورگر ← توکن‌سرور | هر کسی که به نشانی برنامه برسد؛ توکن LiveKit ارزش مالی دارد چون دقیقه‌ی OpenAI Realtime مصرف می‌کند |
| کاربر ← مدل ← ابزارهای ایجنت | آرگومان‌های ابزار را مدل تولید می‌کند و کاربر دیکته می‌کند؛ یعنی ورودی کاربر با یک واسطه |
| ایجنت ← فایل مسدودی | فایلی روی دیسک که وضعیت واقعی دسترسی کاربران را نگه می‌دارد |

---

## محافظت‌های موجود

### توکن‌سرور (`frontend/app/api/connection-details/route.ts`)

- **کد دسترسی** از `ACCESS_CODE`. مقایسه با `timingSafeEqual` انجام می‌شود تا
  مدت پاسخ چیزی از کد لو ندهد.
- **محدودیت نرخ** بر اساس IP، با سقف `RATE_LIMIT_PER_MINUTE` (پیش‌فرض ۱۰).
  درخواست‌های با کد غلط هم سهمیه را مصرف می‌کنند، وگرنه حدس زدن کد نامحدود بود.
- صدور توکن با **POST** انجام می‌شود تا کد در نشانی، لاگ سرور و تاریخچه‌ی
  مرورگر ننشیند.
- شناسه‌ی اتاق و کاربر با `crypto.randomUUID` ساخته می‌شود، نه `Math.random`.
- پیام خطای خام به کلاینت نمی‌رود؛ جزئیات فقط در لاگ سرور می‌ماند.

### ابزارهای ایجنت (`agent/src/guards.py`)

- اعتبارسنجی قالب نشانی ایمیل، و **رد کردن `\r` و `\n`** در نشانی و موضوع تا
  کسی نتواند سرتیتر `Bcc` تزریق کند و رونوشت پنهان بگیرد.
- سقف طول موضوع و متن.
- الگوی دامنه‌ی مجاز از `EMAIL_ALLOWED_DOMAINS`.
- سقف تعداد فراخوانی هر ابزار در هر نشست.
- `unblock_user` فقط همان کاربر نام‌برده را حذف می‌کند.

### فرانت‌اند

هدرهای `Content-Security-Policy`، `Strict-Transport-Security`،
`X-Frame-Options`، `X-Content-Type-Options`، `Referrer-Policy` و
`Permissions-Policy` روی همه‌ی مسیرها تنظیم می‌شوند و `X-Powered-By` حذف شده است.

### یکپارچگی پیوسته

`pnpm audit` و `npm audit` با آستانه‌ی `high` روی هر PR، به‌علاوه‌ی اسکن کل
تاریخچه‌ی گیت برای کلیدهای نشت‌کرده.

---

## ⚠️ محدودیت‌های شناخته‌شده

این‌ها را صریح می‌نویسیم تا کسی فکر نکند مسئله کامل حل شده است.

### ۱. محدودیت نرخ در حافظه است

`frontend/lib/rate-limit.ts` وضعیت را در حافظه‌ی همان فرایند نگه می‌دارد. اگر
برنامه را روی **چند نمونه** یا روی **سرورلس با کولد-استارت** اجرا کنید، هر
نمونه شمارنده‌ی خودش را دارد و سقف واقعی چند برابر می‌شود.

برای استقرار جدی باید به یک ذخیره‌ساز مشترک (Redis، Upstash یا مشابه) منتقل شود،
یا محدودیت را روی لایه‌ی بالاتر (nginx، Cloudflare) بگذارید.

### ۲. تشخیص IP به پروکسی اعتماد می‌کند

کلید محدودیت از `x-forwarded-for` می‌آید. اگر برنامه **مستقیماً** در معرض
اینترنت باشد و پشت پروکسی نباشد، این هدر را خود کلاینت می‌فرستد و کاملاً قابل
جعل است. فقط وقتی معتبر است که یک پروکسی قابل اعتماد آن را بازنویسی کند.

### ۳. کد دسترسی، احراز هویت کاربر نیست

`ACCESS_CODE` یک راز مشترک است، نه هویت. همه‌ی کاربران یک کد دارند، چرخش کد
دستی است، و هیچ ردی از اینکه چه کسی وارد شده نمی‌ماند. برای یک ابزار داخلی
کافی است؛ برای سامانه‌ای که باید بداند چه کسی چه کاری کرده، به احراز هویت واقعی
نیاز دارید.

### ۴. اپ دمو احراز هویت واقعی ندارد

`demo-app/src/pages/Login.tsx` اطلاعات ورود را **هاردکد** دارد و وضعیت ورود را
در `localStorage` نگه می‌دارد. این عمدی است: اپ دمو فقط نقش «نرم‌افزار دارای
مشکل» را برای سناریوی پشتیبانی بازی می‌کند و هیچ داده‌ی واقعی ندارد. **هرگز این
الگو را در سامانه‌ی واقعی استفاده نکنید.**

### ۵. فایل مسدودی، کنترل دسترسی واقعی نیست

`demo-app/public/blockusers.txt` از طریق وب سرو می‌شود و ایجنت آن را روی دیسک
ویرایش می‌کند. هیچ قفل هم‌زمانی و هیچ گزارش تغییری ندارد. باز هم بخشی از دموست،
نه یک سازوکار مجوزدهی.

### ۶. رله‌ی ایمیل بدون الگوی دامنه، باز است

اگر `EMAIL_ALLOWED_DOMAINS` تنظیم نشود، ایجنت می‌تواند از حساب جیمیل شما به هر
نشانی ایمیل بفرستد. سقف تعداد در هر نشست هست، ولی مقصد محدود نیست. ایجنت هنگام
شروع هشدار لاگ می‌کند. **برای استقرار واقعی حتماً تنظیمش کنید.**

### ۷. محدودیت موضوعی، راهنمایی مدل است نه فیلتر

موضوع‌های ممنوعِ تعریف‌شده در پنل، به دستورهای مدل اضافه می‌شوند و در پرامپت
صریح گفته شده که حتی با اصرار یا درخواست نقش‌بازی نادیده گرفته نشوند. با این
حال این یک **مهار احتمالاتی** است، نه یک فیلتر قطعی: مدل زبانی می‌تواند دور زده
شود. اگر به تضمین سخت نیاز دارید، فیلتر کلیدواژه‌ای روی رونوشت یا یک دسته‌بند
جداگانه لازم است — که کار مستقلی است و اینجا انجام نشده.

### ۸. پنل ادمین یک راز مشترک دارد، نه حساب کاربری

همه‌ی ادمین‌ها یک `ADMIN_PASSWORD` مشترک دارند. یعنی:

- هیچ ردی نمی‌ماند که چه کسی چه چیزی را عوض کرده
- عوض کردن رمز یعنی عوض کردنش برای همه
- پنل هیچ گزارش رخداد (audit log) ندارد

مهارهایی که هست: مقایسه‌ی زمان‌ثابت، محدودیت نرخ روی مسیر ورود، کوکی امضاشده‌ی
`HttpOnly` + `SameSite=Strict` + `Secure` با اعتبار هشت ساعت، و یک میدل‌ور که
همه‌ی مسیرهای `/admin` و `/api/admin` را در یک نقطه می‌بندد. پنل را روی اینترنت
باز نگذارید.

---

## حریم خصوصی رونوشت گفت‌وگوها

رونوشت شامل **گفته‌های واقعی کاربر** است، نه لاگ فنی. کاربر ممکن است نام، شماره
تماس یا جزئیات حسابش را بلند بگوید. پس این داده‌ی شخصی است و باید مثل داده‌ی
شخصی با آن رفتار شود:

| مهار | کجا |
|---|---|
| خاموش کردن کامل ثبت رونوشت | پنل ادمین → تنظیمات → «ثبت رونوشت گفت‌وگوها» |
| مدت نگه‌داری، با پاک‌سازی خودکار موارد قدیمی | `transcript_retention_days`، پیش‌فرض ۳۰ روز |
| دسترسی فقط پشت رمز ادمین | میدل‌ور روی `/admin/conversations` |

نکته‌هایی که باید بدانید:

- فایل پایگاه داده **رمزنگاری‌شده نیست**. فقط کلیدهای API داخلش رمز شده‌اند.
  هر کسی که به دیسک سرور دسترسی دارد، رونوشت‌ها را می‌خواند. اگر لازم است،
  رمزنگاری در سطح دیسک یا فایل‌سیستم را خودتان اضافه کنید.
- نسخه‌ی پشتیبان دیسک هم شامل همین رونوشت‌هاست.
- اگر سامانه با کاربران واقعی کار می‌کند، **به آن‌ها اطلاع بدهید** که گفت‌وگو
  ثبت می‌شود؛ در بسیاری از حوزه‌های قضایی این یک الزام قانونی است.
- صدا ذخیره نمی‌شود؛ فقط متن.

---

## متغیرهای محیطی امنیتی

| متغیر | پیش‌فرض | کار |
|---|---|---|
| `ACCESS_CODE` | خالی (غیرفعال) | کد ورود به گفت‌وگو |
| `RATE_LIMIT_PER_MINUTE` | `10` | سقف درخواست توکن در دقیقه، به ازای هر IP |
| `TOKEN_TTL` | `15m` | مدت اعتبار توکن LiveKit |
| `EMAIL_ALLOWED_DOMAINS` | خالی (بدون محدودیت) | دامنه‌های مجاز برای ارسال ایمیل |
| `EMAIL_MAX_PER_SESSION` | `3` | سقف ارسال ایمیل در هر گفت‌وگو |
| `UNBLOCK_MAX_PER_SESSION` | `5` | سقف رفع مسدودیت در هر گفت‌وگو |
| `ADMIN_PASSWORD` | خالی (پنل غیرفعال) | رمز ورود پنل ادمین |
| `ENCRYPTION_KEY` | خالی (پنل بالا نمی‌آید) | کلید ۳۲ بایتی base64 برای رمز کردن کلیدهای API و امضای کوکی ادمین |
| `DATABASE_PATH` | `data/ostandari.db` | فایل مشترک پنل و ایجنت |

---

## چرخش کلیدها

کلیدهای LiveKit، OpenAI، Beyond Presence و رمز اپلیکیشن جیمیل **هرگز** نباید
کامیت شوند. `.gitignore` همه‌ی `.env*` را جز `.env.example` کنار می‌گذارد و CI
تاریخچه را برای کلیدهای نشت‌کرده اسکن می‌کند.

اگر کلیدی لو رفت، **باطل کردنش تنها راه‌حل است** — حذف فایل از تاریخچه کافی
نیست، چون ممکن است قبلاً کپی شده باشد.

### چرخش `ENCRYPTION_KEY`

عوض کردن این کلید، کلیدهای API ذخیره‌شده در پنل را **غیرقابل‌خواندن** می‌کند.
رفتار سامانه در آن حالت عمدی است: ایجنت به‌جای کرش کردن، به متغیرهای محیطی
برمی‌گردد و در لاگ هشدار می‌دهد. کوکی‌های نشست ادمین هم باطل می‌شوند و همه باید
دوباره وارد شوند. بعد از چرخش، کلیدها را در `/admin/keys` دوباره وارد کنید.
