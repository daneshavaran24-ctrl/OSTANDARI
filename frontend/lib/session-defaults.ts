/**
 * مدت پیش‌فرض هر گفت‌وگو، به ثانیه.
 *
 * یک جا تعریف شده چون سه مصرف‌کننده دارد: پنل ادمین، شمارش معکوس مرورگر و
 * پاسخ `/api/session-config`. اگر هرکدام عدد خودش را داشته باشد، نبودِ مقدار
 * در پایگاه داده به سه رفتار متفاوت منجر می‌شود.
 *
 * پنج دقیقه انتخاب شده چون گردش‌کار کامل — پرسش، ارسال ایمیل با تأیید، پرسیدن
 * آدرس، انتخاب شعبه و مسیریابی — در کمتر از آن جا نمی‌شود. برای نمایش کیوسکی
 * مقدار کوتاه‌تر (مثلاً ۳۰ ثانیه) از پنل قابل تنظیم است و پرامپت ایجنت خودش را
 * با مدت کوتاه وفق می‌دهد.
 */
export const DEFAULT_SESSION_SECONDS = 300;

/** زیر این مدت، گردش‌کار کامل جا نمی‌شود و پنل هشدار می‌دهد. */
export const SHORT_SESSION_THRESHOLD = 120;
